Setup : 

Build a requirements file

Install duckdb, onnxruntime, torch, sklearn in an venv

order to run: get the pth model, convert that to onmix, then execute predict_duckdb.py

adjust those paths and run the scripts

